#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

const int AB_MIN = 1, AB_MAX = 4;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    
    int a = inf.readInt(AB_MIN, AB_MAX, "a");
    inf.readSpace();

    int b = inf.readInt(AB_MIN, AB_MAX, "b");
    inf.readEoln();

    inf.readEof();
}
